import sys
import numpy as np
import pymssql as pm

#sys.argv

#def distance_filter:

# 사용자가 입력한 경도와 위도를 받음.
# 한정할 거리를 m단위로 받음.
# DB에서 모든 Circle의 중점으로부터 사용자가 입력한 위치사이의 거리가 원하는 거리 내에 포함되는지 계산.
# 포함되는 원만 뽑아내어 weight을 내림차순으로 정렬
# 출력
print("asgasgasgasg")